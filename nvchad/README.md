The `custom` directory here must be symlinked to `~/.config/nvim/lua/custom` as this folder contains all customization for `NVChad`.

```bash
ln -s ~/.dotfiles/nvchad/custom ~/.config/nvim/lua/custom
```
