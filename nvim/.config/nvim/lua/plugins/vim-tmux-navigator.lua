-- https://github.com/christoomey/vim-tmux-navigator#lazy.nvim
-- Mappings are set in lua/config/keymaps.lua so they load *after* LazyVim’s <C-w> window maps and win.
return {
  "christoomey/vim-tmux-navigator",
  lazy = false,
}
